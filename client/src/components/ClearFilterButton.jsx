export default function ({ handleClick }) {
  return (
    <button
      id="aligned-button-text"
      className="button button-filter-version"
      onClick={handleClick}>
      Clear
    </button>
  );
}
