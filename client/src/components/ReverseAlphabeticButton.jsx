export default function ({ handleClick }) {
  return (
    <button
      id="reverse-alphabetic-button"
      className="filter-option-button"
      onClick={handleClick}>
      Z-A
    </button>
  );
}
